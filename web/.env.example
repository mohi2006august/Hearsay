# Base URL of hearsay-api. Leave unset in development: the Vite dev server
# proxies /api to 127.0.0.1:8787, so the browser stays same-origin.
#
# Set this for a built bundle served from a different origin than the API.
# PUBLIC_HEARSAY_API=http://127.0.0.1:8787
